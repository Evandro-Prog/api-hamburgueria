import app from './app'

app.listen(3000, () => {
    console.log('Server is running on port 3000')
})

// This code starts an Express server on port 3000 and logs a message to the console indicating that the server is running.